# Reference unit for self-hosting on a plain Linux VPS (no Docker).
# Copy to /etc/systemd/system/street-scout-gateway.service, adjust paths for
# your actual deploy location, then:
#   sudo systemctl daemon-reload
#   sudo systemctl enable --now street-scout-gateway

[Unit]
Description=Street Scout Gateway
After=network.target

[Service]
Type=simple
User=streetscout
WorkingDirectory=/opt/street-scout/gateway
EnvironmentFile=/opt/street-scout/gateway/.env
ExecStart=/usr/bin/node src/index.js
Restart=on-failure
RestartSec=5
TimeoutStopSec=15
NoNewPrivileges=true
ProtectSystem=strict
ReadWritePaths=/opt/street-scout/gateway

[Install]
WantedBy=multi-user.target
