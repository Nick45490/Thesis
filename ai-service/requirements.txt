fastapi==0.115.0
uvicorn==0.30.6
python-multipart==0.0.9
python-dotenv==1.0.1
Pillow==12.2.0
# CPU-only wheels, explicit index — a bare `pip install torch` on a fresh
# Linux host can otherwise resolve a multi-GB CUDA build that's both wasteful
# and untested here (this app is CPU-only end to end). Versions below are
# exactly what's validated in the working local dev environment, not guesses.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0+cpu
torchvision==0.27.0+cpu
transformers==4.44.2
ultralytics==8.4.51
faiss-cpu==1.13.2
numpy==2.4.5
ddgs==9.14.4
huggingface_hub==0.36.2
scikit-learn==1.9.0
peft==0.19.1
